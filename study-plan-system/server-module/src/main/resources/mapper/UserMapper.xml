<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.group3.mapper.UserMapper">

    <select id="selectByUsername" resultType="com.group3.common.entity.User" parameterType="java.lang.String">
        SELECT id, username, password, phone, avatar, email, status,
               create_time AS createTime, update_time AS updateTime,
               last_login_time AS lastLoginTime, version
        FROM t_user
        WHERE username = #{username}
    </select>
</mapper>